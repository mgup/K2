<% content_for :title, "Редактирование входящего от #{l(@order.date)} "\
                       "№ #{@order.full_number} " %>

<%= render 'form' %>

<ul class="button-group even-2">
  <li>
    <%= link_to office_document_type_orders_path(@document_type),
                class: 'button secondary' do %>
      <i class="fa fa-arrow-left"></i> Вернуться к зарегистрированным документам
    <% end %>
  </li>
</ul>
